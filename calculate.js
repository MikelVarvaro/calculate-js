const calculate = {
 add:(x, y) => {
    return +x + +y;
  },
  sub: (x, y) => {
    return x - y;
  },
  mul: (x, y) => {
    return x * y;
  },
  pow: (x, y) => {
    return x ** y;
  },
}


window.cal = calculate
